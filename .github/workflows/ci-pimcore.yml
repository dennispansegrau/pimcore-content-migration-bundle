name: CI Pimcore

on:
    pull_request:
        branches: [ main, develop ]
    schedule:
        - cron: "0 0 * * *"

concurrency:
    group: ci-${{ github.ref }}
    cancel-in-progress: true

permissions:
    contents: read

jobs:
    pimcore:
        runs-on: ubuntu-24.04
        timeout-minutes: 45
        env:
            PIMCORE_IMAGE: docker.io/pimcore/pimcore:php8.3-v3.7
            PIMCORE_PACKAGE: pimcore/demo
            COMPOSER_HOME: /tmp/composer
            COMPOSER_CACHE_DIR: /tmp/composer-cache
            PIMCORE_VERSION: "11.5.*"
            COMPOSER_AUDIT_FLAGS: "--no-audit"
        name: Bundle Check
        steps:
            -   name: Checkout
                uses: actions/checkout@v6

            -   name: Pull Pimcore image
                run: |
                    set -ex
                    docker pull ${PIMCORE_IMAGE}

            -   name: Create Pimcore project
                run: |
                    set -ex
                    echo "Using demo_package=${PIMCORE_PACKAGE}"
                    docker run \
                      -e COMPOSER_HOME=${COMPOSER_HOME} \
                      -e COMPOSER_CACHE_DIR=${COMPOSER_CACHE_DIR} \
                      --volume=${{ github.workspace }}/:/var/www/html \
                      --workdir=/var/www/html \
                      --user=$(id -u):$(id -g) \
                      ${PIMCORE_IMAGE} \
                        composer create-project \
                        ${PIMCORE_PACKAGE} \
                        --no-install \
                        --no-scripts \
                        --no-interaction \
                        sample-project
                    docker run \
                      -e COMPOSER_HOME=${COMPOSER_HOME} \
                      -e COMPOSER_CACHE_DIR=${COMPOSER_CACHE_DIR} \
                      -e PIMCORE_VERSION=${PIMCORE_VERSION} \
                      --volume=${{ github.workspace }}/:/var/www/html \
                      --workdir=/var/www/html/sample-project \
                      --user=$(id -u):$(id -g) \
                      ${PIMCORE_IMAGE} \
                        bash -lc 'composer config audit.block-insecure false --no-interaction \
                        && composer remove pimcore/platform-version --no-update --no-interaction \
                        && composer require pimcore/pimcore:"${PIMCORE_VERSION}" --no-update --no-interaction \
                        && composer update --no-scripts --no-interaction ${COMPOSER_AUDIT_FLAGS}'

            -   name: Docker and Database Setup
                run: |
                    set -ex
                    cd sample-project/
                    docker compose -f docker-compose.yaml config

                    docker compose pull --quiet
                    docker compose down -v --remove-orphans
                    docker compose -f docker-compose.yaml up -d

                    docker compose exec -T -- php bash -lc '
                    for attempt in $(seq 1 60); do
                        if (echo > /dev/tcp/db/3306) >/dev/null 2>&1; then
                            exit 0
                        fi
                        sleep 5
                    done
                    echo "Database did not become ready in time" >&2
                    exit 1'

            -   name: Install Pimcore
                run: |
                    set -ex
                    cd sample-project/

                    docker compose exec -T --workdir /var/www/html php composer install --no-scripts --no-interaction
                    
                    docker compose exec -T --workdir /var/www/html \
                        php vendor/bin/pimcore-install \
                        --mysql-host-socket=db \
                        --mysql-username=pimcore \
                        --mysql-password=pimcore \
                        --mysql-database=pimcore \
                        --admin-username=admin \
                        --admin-password=admin \
                        -n

                    docker compose exec -T --workdir /var/www/html \
                        -e DATABASE_URL=mysql://pimcore:pimcore@db:3306/pimcore \
                        php bin/console assets:install --symlink --relative
                    
                    docker compose exec -T --workdir /var/www/html php php -v

            -   name: Install bundle
                run: |
                    set -ex
                    cd sample-project/
                    docker compose exec -T --workdir /var/www/html php composer require dennispansegrau/pimcore-content-migration-bundle:dev-develop

            -   name: Install bundle dependencies
                run: |
                    set -ex
                    cd sample-project/
                    docker compose exec -T --workdir /var/www/html/vendor/dennispansegrau/pimcore-content-migration-bundle php \
                        bash -lc 'composer install --no-interaction --prefer-dist;'

            -   name: PHPStan Test
                run: |
                    set -ex
                    cd sample-project/
                    docker compose exec -T --workdir /var/www/html/vendor/dennispansegrau/pimcore-content-migration-bundle php \
                        bash -lc 'composer phpstan'

            -   name: CS Test
                run: |
                    set -ex
                    cd sample-project/
                    docker compose exec -T --workdir /var/www/html/vendor/dennispansegrau/pimcore-content-migration-bundle php \
                        bash -lc 'composer cs'

            -   name: PHPUnit Tests
                run: |
                    set -ex
                    cd sample-project/
                    docker compose exec -T --workdir /var/www/html/vendor/dennispansegrau/pimcore-content-migration-bundle php \
                        bash -lc 'composer test'

            -   name: Compose down
                if: always()
                run: |
                    cd sample-project/
                    docker compose down -v --remove-orphans
